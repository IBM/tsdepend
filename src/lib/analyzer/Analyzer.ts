export abstract class Analyzer {}

// Ideas for more Analyzers:
// assertEquals(36, p.getConcreteClassCount()); => ConcreteClassCountAnalyzer
// assertEquals(7, p.getAbstractClassCount()); => AbstractClassCountAnalyzer
// assertEquals(0, p.afferentCoupling());
// assertEquals(5, p.efferentCoupling());
// => some metris are also described in the arch.-metrics video from software architecture monday video series, would be a good link and description for these implementations
